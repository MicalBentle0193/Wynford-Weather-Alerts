workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - flutter pub get
      - cd ios
      - pod install --repo-update
    artifacts:
      - build/ios/ipa/*.ipa
